{pkgs ?import <nixpkgs>{}}
{pkgs ? import <nixpkgs>{}}
<pkgs ? impor
myFunction = arg:expression;

greet =name:"hello,+name

pkgs.stdenv.mkDerivation{
    name="package-name";
    src=...;

}
pkgs.stdenv.mkdDerivation{
    name="package name";
    src=...;
}
